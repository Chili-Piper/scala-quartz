# This workflow will launch at 00:00 every Sunday
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

name: Launch Scala Steward

jobs:
  scala-steward:
    runs-on: [cp-small]
    name: Launch Scala Steward
    steps:
      - name: Setup jdk
        uses: coursier/setup-action@v1.3.9
        with:
          jvm: temurin:1.17

      - name: Launch Scala Steward
        uses: scala-steward-org/scala-steward-action@v2
        with:
          github-token: ${{ secrets.SCALA_STEWARD }}
          author-email: "scala-steward@chilipiper.com"
          author-name: "Scala Steward Bot"
